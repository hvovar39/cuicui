// vérifie s'il y a une ligne suivante
int hasNext(ligne * l);
// vérifie s'il y a une ligne précédente
int hasPrev(ligne * l);
// renvoie la ligne du buffer courant associée au numéro
ligne * prendreLigne(int n);
// renvoie la taille d'une ligne
int tailleLigne(ligne * l);
// renvoie la dernière ligne d'un buffer à partir d'une autre ligne
ligne * derniereLigne(ligne *l);



